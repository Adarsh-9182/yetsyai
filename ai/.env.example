# Copy to .env (or export directly) before running the NutritiScan AI backend.

# Your Anthropic API key — get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# NUTRITISCAN_MODEL=claude-opus-5   # the AI-doctor reasoning model (default: claude-opus-5)
# PORT=8787
